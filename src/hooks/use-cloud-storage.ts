import {
  getAuth,
  GoogleAuthProvider,
  UserCredential,
  signInWithPopup,
} from "firebase/auth";

export const useCloudStorage = () => {
  const signIn = async () => {};

  return { signIn };
};
